{
  "name": "target",
  "type": "module",
  "version": "0.0.0",
  "private": true,
  "description": "Minimal target project for testing agents-sync end-to-end.",
  "scripts": {
    "sync:skill": "agents-sync skill",
    "sync:rule": "agents-sync rule",
    "sync:mcp": "agents-sync mcp",
    "sync:all": "pnpm run sync:skill && pnpm run sync:rule && pnpm run sync:mcp",
    "sync:dry-run": "agents-sync skill --dry-run --yes",
    "sync:reset": "rm -rf .agents .cursor"
  },
  "dependencies": {
    "@tofrankie/agents-sync": "workspace:*"
  }
}
