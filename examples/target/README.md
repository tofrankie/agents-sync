# Example Target

This package is a minimal sync target.

## Run

From this directory:

```bash
pnpm run sync:reset
pnpm run sync:all
```

Or run step by step:

```bash
pnpm run sync:skill
pnpm run sync:rule
pnpm run sync:mcp
```

The source defaults to `../source` via `agents-sync.config.ts`.
